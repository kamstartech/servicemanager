model ExternalBank {
  id        String   @id @default(cuid())
  name      String
  code      String   @unique // Sort Code
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("external_banks")
}
