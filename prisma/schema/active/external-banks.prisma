model ExternalBank {
  id              String           @id @default(cuid())
  name            String
  code            String           @unique // Sort Code
  institutionCode String?
  type            ExternalBankType @default(BANK)
  isActive        Boolean          @default(true)
  createdAt       DateTime         @default(now())

  @@map("external_banks")
}
